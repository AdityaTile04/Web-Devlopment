<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Roll Dice</title>
  </head>
  <body>
    <h1>Dice give value : <%= dice %></h1>

    <!--! Conditional Statements in ejs -->
    <% if(dice == 6) { %>
    <h2>Nice Roll dice again.</h2>
    <% } %> <% if(dice == 1) { %>
    <h2>Opps! Try again</h2>
    <% } %>
  </body>
</html>
